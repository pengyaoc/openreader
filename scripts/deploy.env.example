# Copy this file to deploy.env (gitignored) and fill in your own VM's
# details. scripts/deploy.sh sources it — see that file's header comment.

ZONE="us-west1-a"
PROJECT="your-gcp-project-id"
VM="your-vm-name"
DEPLOY_URL="https://your-domain.example.com/reader/"
